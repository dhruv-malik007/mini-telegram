# API server URL. No trailing slash.
# - Local dev: set to http://localhost:3001 so Run/Submit work (app runs on 5173, API on 3001).
# - Production/Android: set to your deployed server (e.g. https://your-server.example.com).
# VITE_API_URL=http://localhost:3001
