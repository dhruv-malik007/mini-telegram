# Set this when building the Android app (or production web) so the app connects to your server.
# No trailing slash. Leave unset for local dev (Vite proxy to localhost:3001).
# VITE_API_URL=https://your-server.example.com
