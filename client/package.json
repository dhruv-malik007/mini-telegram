{
  "name": "mini-telegram-client",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:android": "vite build && cap sync android",
    "android": "cap open android",
    "apk": "cap sync android && cd android && ./gradlew assembleDebug && cd .."
  },
  "dependencies": {
    "@capacitor/android": "^6.0.2",
    "@capacitor/core": "^6.0.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "socket.io-client": "^4.7.5"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.2",
    "@vitejs/plugin-react": "^4.3.3",
    "vite": "^5.4.10"
  },
  "capacitor": {
    "android": {
      "path": "android"
    }
  }
}
